name: photo_search
description: Demo Search Application


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  animated_text_kit: ^1.3.0
  cupertino_icons: ^1.0.2
  flutter_bloc: ^6.0.1
  http: ^0.12.0+2
  image_downloader: ^0.30.0
  internet_connection_checker: ^0.0.1+3
  pinch_zoom: ^1.0.0
  pull_to_refresh: ^1.6.5
  rflutter_alert: ^1.0.3
  splashscreen: ^1.3.5


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
     - assets/